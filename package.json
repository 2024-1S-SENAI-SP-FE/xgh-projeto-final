{
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  },
  "scripts": {
    "start": "npx json-server --watch data/users.json --port 3000"
  },
  "devDependencies": {
    "cypress": "^13.12.0"
  }
}
